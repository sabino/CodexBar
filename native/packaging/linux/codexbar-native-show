#!/bin/sh
set -eu

cleanup() {
    systemctl --user unset-environment CODEXBAR_SHOW_ON_START >/dev/null 2>&1 || true
}

trap cleanup EXIT HUP INT TERM
systemctl --user set-environment CODEXBAR_SHOW_ON_START=1
systemctl --user restart codexbar-native.service
